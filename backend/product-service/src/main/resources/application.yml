server:
  port: 8081

spring:
  application:
    name: product-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        ip: 127.0.0.1

management:
  endpoints:
    web:
      exposure:
        include: health,info

info:
  app:
    name: pinshengsheng-product-service
    description: 拼省省商品服务
